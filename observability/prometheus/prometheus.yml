global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "bilibili-music"
    metrics_path: "/actuator/prometheus"
    static_configs:
      # If the app runs on the host machine, Docker on Windows/macOS supports host.docker.internal.
      - targets: ["host.docker.internal:8080"]

